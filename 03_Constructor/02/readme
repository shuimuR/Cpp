
1. new Person()返回的时Person的指针，因此，接收他的是Person *per的对象

2. 在使用new创建的对象，使用完之后要主动调用delete释放对象，此时会调用析构函数，如果不主动delete，在程序结束时，系统会释放对象，但是不会调用析构函数;

3. 不使用new创建的对象，直接创建，如Person per1("Liming")，在函数结束时，系统会主动调用其析构函数;

4. 在调用对象时有分配内存的，在析构函数中要释放其分配的内存;


